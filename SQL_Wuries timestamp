SELECT DATE, RESOURCE_ID, MIN(START_TIME) AS CLOCK_IN_TIME, MAX(END_TIME) AS CLOCK_OUT_TIME, TIMESTAMPDIFF(HOUR, START_TIME, END_TIME) AS TOTAL_HOURS 
FROM STOP_WATCH_DATA
GROUP BY DATE, RESOURCE_ID;
